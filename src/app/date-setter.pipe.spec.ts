import { DateSetterPipe } from './date-setter.pipe';

describe('DateSetterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSetterPipe();
    expect(pipe).toBeTruthy();
  });
});
